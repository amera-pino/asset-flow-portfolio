# React Study Log

## Date

- 2026-07-02

## Purpose

- React / TypeScript / TSX の復習記録を残す
- このアプリのコードの流れを自力で説明できる状態に近づける

## Reviewed Today

- 5分程度の短めテストを実施
- `frontend-study-log` 配下で学習ログを運用する方針を確認
- `main.tsx` の描画処理を確認
- `App()` の `Routes` / `Route` によるルーティング設定を確認
- `AssetListPage` が備品一覧画面を描画するコンポーネントであることを確認
- 実際に書く練習だけでなく、穴埋め問題も混ぜる方針を確認

## Questions Asked Today

- `ReactDOM.createRoot(document.getElementById("root")!).render(<App />)` の空欄補完問題
- `App()` が何をしているかを説明する問題
- `AssetListPage` の役割を説明する問題

## Understood

- `ReactDOM.createRoot(document.getElementById("root")!).render(<App />)` で `root` に `App` を描画する
- `App()` は `BrowserRouter` / `Routes` / `Route` を使って URL ごとの画面を切り替える
- `AssetListPage` は備品一覧画面を描画する React コンポーネント
- `script` タグ、`root` 要素、`main.tsx`、`App()` のつながりを少しずつ説明できる

## Still Fuzzy

- `createRoot` と `getElementById` を取り違えやすい
- `App()` の役割を、短くきれいに言い切る練習を増やしたい
- `Routes` と `Route` の名前がまだあいまいになりやすい

## Next Time

- `main.tsx` から `App()` までの流れをもう一度短く確認する
- `createRoot` / `getElementById` / `render` を穴埋めで再確認する
- 次は `AssetListPage` とその周辺をもう少し深く見る

## Notes For Quiz Design

- 書く問題だけでなく、穴埋め問題を混ぜる
- `createRoot` や `getElementById` など、重要キーワードを空欄にして書かせる
- 全文をゼロから書かせるより、重要部分を切り出して確認する
- 実際の実装に近い形で、切り貼り前提の出題も取り入れる
