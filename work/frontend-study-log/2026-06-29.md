# React Study Log

## Date

- 2026-06-29

## Purpose

- React / TypeScript / TSX の復習記録を残す
- このアプリのコードの流れを自力で説明できる状態に近づける

## Reviewed Today

- 15分の短めテストを実施
- `index.html` の入口部分を確認
- `script` タグと `root` 要素の役割を切り分けて確認
- `head` 内のタグは主役ではないが、補助問題として出してよい方針を確認
- タグや属性値の穴埋め問題も今後混ぜる案を追加

## Questions Asked Today

- `main.tsx` を読み込む `script` タグを書く問題
- `<script type="module" src="/src/main.tsx"></script>` の役割を説明する問題
- `index.html` の次につながるファイル名を答える問題
- React の描画先となる `<div id="root"></div>` を書く問題

## Understood

- `index.html` の次につながるのは `main.tsx`
- `<script type="module" src="/src/main.tsx"></script>` は `main.tsx` をモジュールとして読み込み、フロントエンド処理を開始するためのタグ
- `<div id="root"></div>` は React が画面を描画する土台
- `script` タグの役割と `root` 要素の役割は分けて説明する必要がある

## Still Fuzzy

- `script` タグの完全な形は、まだ即答の安定感を上げたい
- `script` タグの役割説明で、`root` 要素の話と混ざりやすい

## Next Time

- `index.html` をもう一度だけ短く確認する
- `script` タグと `root` 要素を再出題する
- 定着が見えたら `main.tsx` に進む

## Notes For Quiz Design

- 数日後に同じ問題を再出題して定着確認する
- `head` 内タグは主軸にはせず、補助問題として扱う
- タグ名や属性値の穴埋め問題もときどき混ぜる
