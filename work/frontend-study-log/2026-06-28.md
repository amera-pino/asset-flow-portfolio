# React Study Log

## Date

- 2026-06-28

## Purpose

- React / TypeScript / TSX の復習記録を残す
- このアプリのコードの流れを自力で説明できる状態に近づける

## Reviewed Today

- `asset-flow` の学習用作業フォルダを作成
- `work/frontend-study-log/` の運用を開始
- 学習用ブランチで進める方針を確認
- `Object.entries(query ?? {})` を題材に、軽い練習問題を実施
- 今後の出題順は `index.html -> main.tsx -> App() -> AssetListPage.tsx -> api.ts` を基本にする方針を確認
- 毎日テスト前に `study-policy.md` と直近ログを読む運用を決定

## Understood

- 学習用メモは `asset-flow/work/frontend-study-log/` に置く
- 小テストはまず `3問前後の短め` を基本にする
- 実コードを読みながら、`部品` `接続` `流れ` を混ぜて復習する
- `query ?? {}` は、`query` が `null` または `undefined` のときに `{}` を使うための書き方
- `Object.entries({ page: 2, category: "pc" })` は `[[\"page\", 2], [\"category\", \"pc\"]]` の形になる

## Still Fuzzy

- `Object.entries` の説明で、`返す` と `渡す` の言い分けがまだ少し曖昧
- `Object.entries` の結果が `[[キー, 値], ...]` という形であることを反射的に書けるようにしたい
- 本格的な React 復習テストは明日から開始

## Next Time

- `study-policy.md` と直近ログを読んでから開始する
- `index.html` に絞った `書く / 説明する / 接続または流れ` の 2〜3 問を出題する
- 必要なら `Object.entries` を 1 問だけ再確認する

## Notes For Quiz Design

- 1時間で終わるペースを優先する
- 短くても考える必要がある問題にする
- 回答が安定してきたら `5〜7問` に増やす
- 基本は流れ順で出題し、曖昧な理解のまま先へ進みすぎない
